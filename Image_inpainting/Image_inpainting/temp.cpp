/*
vector<Point2i>candidate;
int candidate_size = area_size[area_index] * level_set_radio;
sort(points.begin(), points.end(), compare);
if (points.size() > candidate_size) {
candidate.assign(points.begin(), points.begin() + candidate_size);
}
else {
candidate.assign

}
*/